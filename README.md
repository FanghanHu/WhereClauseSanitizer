# WhereClauseSanitizer

this project contains an SQL function that will accept a where clause string provided by an user, then sanitize it and return the sanitized whereclause and parameterized values with a table variable.
